package m1.exail

import m1.exail.model.getDroneAddress
import m1.exail.model.FTPHandler
import m1.exail.view.MainFrame

fun main() {
    // val droneAddress = getDroneAddress()
    // val ftpHandler = FTPHandler(droneAddress)
    MainFrame()
    // for (f in ftpHandler.listFiles()) {
    //     println(f)
    // }
}